# HD1 World Two - Minimalist Blue Sphere Scene
# Scene 2: Blue Sphere + Overhead Light + Earth Environment

world:
  id: "world_two"
  name: "Scene 2 - Blue Sphere"
  description: "Minimalist scene with blue sphere and overhead lighting"
  max_clients: 100

# Native PlayCanvas Scene Configuration
playcanvas:
  # Global scene settings - Earth-like environment
  scene:
    ambientLight: [0.3, 0.3, 0.3]  # RGB values 0-1 (soft ambient)
    gravity: [0, -9.81, 0]          # Earth gravity
    
  # Minimal entities: blue sphere + overhead light + floor
  entities:
    - name: "floor"
      components:
        model:
          type: "plane"
        transform:
          position: [0, -0.1, 0]  # Slightly below origin
          rotation: [0, 0, 0]     # No rotation - flat horizontal
          scale: [10, 1, 10]      # 10x1x10 floor (10 wide, 1 tall, 10 deep)
        material:
          diffuse: "#cccccc"      # Light gray floor
          metalness: 0.1
          roughness: 0.8
        rigidbody:
          type: "static"
        collision:
          type: "box"
          
    - name: "blue_sphere"
      components:
        model:
          type: "sphere"
        transform:
          position: [0, 1, 0]     # 1 unit above floor
          scale: [1, 1, 1]        # 1 meter radius sphere
        material:
          diffuse: "#0066ff"     # Pure blue
          metalness: 0.0         # Non-metallic for better color
          roughness: 0.6
          shininess: 50
        rigidbody:
          type: "dynamic"
          mass: 1.0
        collision:
          type: "sphere"
          
    - name: "overhead_light"
      components:
        light:
          type: "directional"
          color: [1.0, 1.0, 1.0]  # RGB values 0-1 (white)
          intensity: 3.0          # Intensity 0-10 range
          castShadows: true
          enabled: true           # Explicitly enable
        transform:
          position: [0, 10, 5]    # Position for reference
          rotation: [60, 30, 0]   # More direct downward angle