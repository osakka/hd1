# HD1 Avatar Specification - Humanoid Avatar
# Single Source of Truth for Humanoid Avatar Assets

metadata:
  name: "Humanoid Avatar"
  description: "Standard humanoid avatar for general use - CesiumMan robot model"
  version: "1.0.0"
  type: "session-avatar"
  created: "2025-07-05"
  attribution: "Khronos glTF Sample Model - CesiumMan"

# Asset References (relative to avatar directory)
assets:
  model: "model.glb"           # 3D humanoid robot model
  textures: []                 # Embedded in GLB
  animations: []               # Embedded in GLB model

# Avatar Physical Properties
physical:
  height: 1.8                  # Standard human height in meters
  mass: 70                     # Standard mass in kg
  collision_shape: "capsule"   # Simple capsule collision
  
  bounding_box:
    width: 0.6                 # Shoulder width
    height: 1.8                # Full height
    depth: 0.3                 # Front-to-back depth

# Camera Configuration
camera:
  position: {x: 0, y: 1.7, z: 0}  # Eye level for humanoid
  field_of_view: 75                # Standard FOV
  perspective: "first-person"       # Default camera mode

# Movement & Animation
movement:
  walk_speed: 5.0              # m/s walking speed
  run_speed: 8.0               # m/s running speed
  jump_force: 300              # Jump impulse force

# Network sync settings
network:
  priority: "high"             # High priority for user avatars
  update_frequency: 30         # 30 Hz position updates
  
  interpolation:
    enabled: true              # Smooth movement interpolation
    method: "linear"           # Linear interpolation
    time: 0.1                  # 100ms interpolation window